version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - npm ci
    build:
      commands:
        - ng build
  artifacts:
    baseDirectory: dist/worldofash/broswer
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*

