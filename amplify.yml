frontend:
  phases:
    preBuild:
      commands:
        - npx ng version
    build:
      commands:
        - ng build --configuration production
  artifacts:
    baseDirectory: dist/worldofash/browser  # Corrected typo here
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
